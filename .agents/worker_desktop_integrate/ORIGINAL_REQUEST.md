## 2026-07-19T17:33:20Z

You are a Desktop Receiver developer worker. Your task is to write and configure the desktop receiver files, compile them on macOS, and verify that they build and pass CTest.

## Actions to perform
1. Move or copy the existing `desktop/windows/src/rtp_hevc.h` and `desktop/windows/src/rtp_hevc.cpp` into a new directory `desktop/core/`.
2. Create and write the following files under `desktop/` with the provided contents:

### desktop/CMakeLists.txt
... (as in prompt)
